/* ===== HERO ROLE TEXT (STATIC) ===== */

const heroRoleElement = document.querySelector('.hero-role');

if (heroRoleElement) {
    heroRoleElement.textContent = 'Java Developer | Full Stack Web Developer';
}
